## Overview
This repository offers some examples of beautiful graphs you can build with Recharts. I'll expand it as I have time.

## Getting Started
In the project directory, run `npm install`. Then, run `npm start` and open your localhost to view the app in your browser.